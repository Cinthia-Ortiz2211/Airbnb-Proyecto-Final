package contract;

/**
 * Interfaz que define un método para obtener el identificador único de un objeto.
 */
public interface Identificable {

    /**
     * Obtiene el identificador único del objeto.
     *
     * @return El identificador único como un entero.
     */
    int getId();
}
